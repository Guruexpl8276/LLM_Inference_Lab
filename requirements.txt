# LLM Inference Lab — Dependencies
# Python 3.10+ recommended. Install with: pip install -r requirements.txt

# API Backend (ASGI)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP client (Ollama streaming calls)
httpx>=0.27.0

# Data validation
pydantic>=2.7.0

# GPU monitoring — nvidia-ml-py is the current maintained package (replaces pynvml)
nvidia-ml-py>=12.0.0

# System metrics
psutil>=5.9.0

# SSE streaming support
sse-starlette>=2.1.0

# Production UI (Hugging Face Spaces / local Gradio)
gradio>=4.36.0

# Utilities
python-dotenv>=1.0.0
aiofiles>=23.2.1

# Persistent research log (SQLite, async)
aiosqlite>=0.20.0
